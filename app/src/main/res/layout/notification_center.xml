<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Title (e.g., Congratulations or Sorry) -->
    <TextView
        android:id="@+id/notificationTitle"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Notification Title" />  <!-- Placeholder text -->

    <!-- Message (e.g., You've won the lottery or Better luck next time) -->
    <TextView
        android:id="@+id/notificationMessage"
        android:textSize="14sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Notification message will go here" />  <!-- Placeholder text -->
        
</LinearLayout>
